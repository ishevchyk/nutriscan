Create a new FastAPI router for the domain: $ARGUMENTS

- Create app/routers/{domain}.py with CRUD structure
- Create app/schemas/{domain}.py with Pydantic v2 models
- Register the router in app/main.py
- Follow async SQLAlchemy patterns used in existing routers
